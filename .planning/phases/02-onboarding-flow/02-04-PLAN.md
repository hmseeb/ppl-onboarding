---
phase: 02-onboarding-flow
plan: 04
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - components/onboarding/steps/Step6Policy.tsx
  - components/onboarding/steps/Step7Confirm.tsx
  - components/onboarding/OnboardingStepper.tsx
autonomous: true
requirements:
  - ONBD-08
  - ONBD-09

must_haves:
  truths:
    - "Step 6 shows replacement policy with checkbox that gates Next button"
    - "Step 7 shows combined summary of webhook data + onboarding selections"
    - "Step 7 primary CTA links to badaaas.com"
    - "Step 7 secondary link shows Daniel's phone number (+1 (702) 412-9233)"
    - "Next button on Step 6 is disabled until checkbox is checked"
    - "Completing Step 7 triggers the completion API call"
  artifacts:
    - path: "components/onboarding/steps/Step6Policy.tsx"
      provides: "Replacement policy with checkbox gate"
      contains: "checkbox"
    - path: "components/onboarding/steps/Step7Confirm.tsx"
      provides: "Final confirmation with summary and CTAs"
      contains: "badaaas.com"
  key_links:
    - from: "components/onboarding/steps/Step7Confirm.tsx"
      to: "OnboardingStepper.handleComplete"
      via: "onComplete callback prop"
      pattern: "onComplete"
    - from: "components/onboarding/OnboardingStepper.tsx"
      to: "/api/brokers/[token]/complete"
      via: "POST fetch in handleComplete"
      pattern: "fetch.*complete"
---

<objective>
Build Step 6 (Replacement Policy with checkbox gate) and Step 7 (Confirmation + completion trigger), then wire them into the OnboardingStepper to complete the full 7-step flow.

Purpose: These are the final two steps that complete the onboarding experience. Step 6 requires policy acknowledgment before proceeding. Step 7 assembles all collected data into a summary and triggers the completion API + GHL webhook.
Output: Complete 7-step onboarding flow — broker can go from Step 1 to completion.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-onboarding-flow/02-RESEARCH.md
@.planning/phases/02-onboarding-flow/02-02-SUMMARY.md
@lib/types.ts
@PPL Onboarding Prompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Step 6 (Replacement Policy) and Step 7 (Confirmation)</name>
  <files>
    components/onboarding/steps/Step6Policy.tsx
    components/onboarding/steps/Step7Confirm.tsx
  </files>
  <action>
1. Create `components/onboarding/steps/Step6Policy.tsx` (ONBD-08):
   - "use client"
   - Import `useState` from react
   - Import shadcn: Button, Card, CardContent, Checkbox, Label
   - Props: `{ onNext: () => void; onBack: () => void }`
   - State: `accepted` boolean (default false)
   - Layout — ALL COPY FROM THE SPEC:
     - Headline: "Our Guarantee"
     - Policy text in a Card:
       "If you receive a referral with a disconnected number, fake information, or someone who clearly never requested funding — we replace it. No questions asked.

       Just flag it in your dashboard or text your rep.

       We don't make money on bad leads. We make money when you close and keep coming back."
     - Checkbox with Label: "I understand the referral replacement policy"
       - Use shadcn Checkbox component
       - On change: toggle `accepted` state
     - Navigation:
       - "Back" button (secondary, always enabled)
       - "Next" button (primary red) — DISABLED until `accepted` is true
       - Use `disabled` prop on the Button: `disabled={!accepted}`
       - Visually, disabled button should appear faded/muted
     - Min-h-[44px] on all buttons and checkbox area

2. Create `components/onboarding/steps/Step7Confirm.tsx` (ONBD-09):
   - "use client"
   - Import `useState` from react
   - Import `Broker` from `@/lib/types`
   - Import `formatPhone` from `@/lib/utils/normalize`
   - Import shadcn: Button, Card, CardContent, Badge, Separator
   - Props: `{ broker: Broker; formData: Record<string, unknown>; onComplete: () => void; onBack: () => void }`
   - State: `isSubmitting` boolean (default false) — prevent double-submit
   - Merge broker data with formData to get final display values:
     - Name: formData.first_name ?? broker.first_name + " " + formData.last_name ?? broker.last_name
     - Company: formData.company_name ?? broker.company_name
     - Email: formData.email ?? broker.email
     - Phone: formData.phone ?? broker.phone (display with formatPhone)
     - Vertical(s): broker.primary_vertical (and secondary if present) — use Badge
     - Delivery method: formData.delivery_method ?? broker.delivery_method (display as human-readable: "SMS", "Email", "CRM Webhook")
     - Batch size: broker.batch_size
   - Layout — ALL COPY FROM THE SPEC:
     - Headline: "You're All Set, {firstName}. Go Fund Some Deals."
     - Summary Card showing all the merged data above (combining webhook data + onboarding selections)
     - Subtext: "Your {batch_size} referrals are on the way. Watch your {deliveryMethodDisplay} — and remember, speed to lead wins."
       - Where {deliveryMethodDisplay} is a human-readable version of the selected delivery method: "texts" for SMS, "email" for email, "CRM" for crm_webhook
     - Primary CTA: "Go to Dashboard" — `<a href="https://badaaas.com">` styled as a Button (full width, red accent, min-h-[44px])
       - On click BEFORE navigating: call `onComplete()` to trigger the completion API
       - Use onClick handler that: sets isSubmitting=true, calls onComplete(), then after a short delay navigates to badaaas.com
       - OR: call onComplete first, and render the link after completion
     - Secondary link: "Questions? Text Daniel at +1 (702) 412-9233"
       - Use `<a href="sms:+17024129233">` for the phone link
     - "Back" button (secondary, subtle) to go back to Step 6
   - IMPORTANT: The "Go to Dashboard" CTA must trigger the completion flow (POST to /api/brokers/[token]/complete) before navigating away. Handle this by:
     - onClick: set isSubmitting, call `onComplete()`, wait for it to resolve, then `window.location.href = 'https://badaaas.com'`
     - Show loading state on button while submitting
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-onboarding && npx tsc --noEmit 2>&1 | head -20 && echo "---" && ls components/onboarding/steps/Step6Policy.tsx components/onboarding/steps/Step7Confirm.tsx</automated>
  </verify>
  <done>
    - Step 6 displays replacement policy with checkbox that disables Next until checked
    - Step 7 displays combined summary of broker data + onboarding selections
    - Step 7 CTA links to badaaas.com and triggers completion API
    - Step 7 shows Daniel's contact info (+1 (702) 412-9233)
    - All copy matches spec exactly
    - Double-submit protection via isSubmitting state
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Steps 6-7 into OnboardingStepper and finalize completion flow</name>
  <files>
    components/onboarding/OnboardingStepper.tsx
  </files>
  <action>
1. Update `components/onboarding/OnboardingStepper.tsx`:
   - Add imports for Step6Policy and Step7Confirm
   - Remove the remaining placeholder block for steps 6-7
   - Add step rendering:
     ```
     {currentStep === 6 && <Step6Policy onNext={() => handleNext()} onBack={handleBack} />}
     {currentStep === 7 && (
       <Step7Confirm
         broker={broker}
         formData={formData}
         onComplete={handleComplete}
         onBack={handleBack}
       />
     )}
     ```
   - Update `handleComplete` to be async and return a Promise so Step7Confirm can await it:
     ```
     const handleComplete = useCallback(async () => {
       const response = await fetch(`/api/brokers/${token}/complete`, {
         method: 'POST',
         headers: { 'Content-Type': 'application/json' },
         body: JSON.stringify({ formData }),
       })
       if (!response.ok) {
         console.error('Completion failed:', response.status)
       }
     }, [token, formData])
     ```
     Note: Do NOT redirect from handleComplete — Step7Confirm handles the navigation to badaaas.com after completion.
   - Verify ALL 7 steps are now wired:
     - Step 1: Step1Welcome (broker, onNext)
     - Step 2: Step2Delivery (broker, onNext, onBack)
     - Step 3: Step3HowItWorks (onNext, onBack)
     - Step 4: Step4ROI (broker, onNext, onBack)
     - Step 5: Step5BestPractices (onNext, onBack)
     - Step 6: Step6Policy (onNext, onBack)
     - Step 7: Step7Confirm (broker, formData, onComplete, onBack)
   - Remove any remaining placeholder/fallback blocks
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-onboarding && npx tsc --noEmit 2>&1 | head -20 && echo "---" && grep -c "Step[1-7]" components/onboarding/OnboardingStepper.tsx</automated>
    <manual>OnboardingStepper imports and renders all 7 step components</manual>
  </verify>
  <done>
    - OnboardingStepper renders all 7 steps with proper navigation
    - No placeholder blocks remain
    - handleComplete posts to /api/brokers/[token]/complete with accumulated formData
    - Full flow: Step 1 -> Step 2 -> ... -> Step 7 -> completion API -> badaaas.com redirect
    - Step persistence (current_step) works on every step transition
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. All 7 steps render in sequence with proper navigation
3. Step 6 checkbox gates progression to Step 7
4. Step 7 shows merged summary data and triggers completion on CTA click
5. handleComplete posts to /api/brokers/[token]/complete
6. No placeholder steps remain in OnboardingStepper
</verification>

<success_criteria>
- Complete 7-step flow from Welcome to Confirmation with no gaps
- Step 6 Next button disabled until policy checkbox is checked
- Step 7 triggers completion API before navigating to badaaas.com
- All copy matches original spec exactly
- Broker can navigate back and forth through all 7 steps
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-flow/02-04-SUMMARY.md`
</output>
