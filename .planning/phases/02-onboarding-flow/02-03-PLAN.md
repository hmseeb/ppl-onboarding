---
phase: 02-onboarding-flow
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - components/onboarding/steps/Step3HowItWorks.tsx
  - components/onboarding/steps/Step4ROI.tsx
  - components/onboarding/steps/Step5BestPractices.tsx
  - components/onboarding/OnboardingStepper.tsx
autonomous: true
requirements:
  - ONBD-05
  - ONBD-06
  - ONBD-07
  - DSGN-05
  - DSGN-06

must_haves:
  truths:
    - "Step 3 displays 5-step referral process timeline with exact spec copy"
    - "Step 4 shows personalized ROI calculator using broker's actual deal_amount and batch_size"
    - "Step 4 ROI calculation handles null/zero values gracefully"
    - "Step 5 displays 5 numbered best practices cards with exact spec copy"
    - "All copy uses direct, confident, money-motivated tone"
    - "ROI numbers use red accent color and large text to visually pop"
  artifacts:
    - path: "components/onboarding/steps/Step3HowItWorks.tsx"
      provides: "Referral process education screen"
      min_lines: 40
    - path: "components/onboarding/steps/Step4ROI.tsx"
      provides: "ROI calculator with personalized numbers"
      contains: "deal_amount"
    - path: "components/onboarding/steps/Step5BestPractices.tsx"
      provides: "5 numbered best practices cards"
      min_lines: 40
  key_links:
    - from: "components/onboarding/steps/Step4ROI.tsx"
      to: "broker.deal_amount / broker.batch_size"
      via: "dynamic ROI calculation"
      pattern: "deal_amount.*batch_size"
    - from: "components/onboarding/OnboardingStepper.tsx"
      to: "Step3HowItWorks, Step4ROI, Step5BestPractices"
      via: "import and render in step switch"
      pattern: "Step3|Step4|Step5"
---

<objective>
Build the three content/education steps (3, 4, 5) and wire them into the OnboardingStepper. Step 3 is a visual timeline of how referrals work, Step 4 has the personalized ROI calculator using actual broker data, and Step 5 shows best practices for closing.

Purpose: These content steps educate brokers and set expectations. Step 4's personalized ROI calculator is the highest-impact visual in the entire flow — it uses real deal_amount and batch_size to show brokers the math works in their favor.
Output: Three content step components wired into the stepper, replacing placeholders.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-onboarding-flow/02-RESEARCH.md
@.planning/phases/02-onboarding-flow/02-02-SUMMARY.md
@lib/types.ts
@PPL Onboarding Prompt.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Step 3 (How Referrals Work) and Step 5 (Best Practices)</name>
  <files>
    components/onboarding/steps/Step3HowItWorks.tsx
    components/onboarding/steps/Step5BestPractices.tsx
  </files>
  <action>
1. Create `components/onboarding/steps/Step3HowItWorks.tsx` (ONBD-05):
   - "use client"
   - Import shadcn: Button, Card, CardContent
   - Props: `{ onNext: () => void; onBack: () => void }`
   - No form state needed — pure content/education screen
   - Layout — ALL COPY MUST BE EXACTLY AS SPECIFIED IN THE PROMPT:
     - Headline: "Here's How This Works"
     - 5-step visual timeline using Card components, numbered 1-5. Each card has a number badge (styled circle with red accent) and the description text:
       1. "A business owner requests funding through our sites"
       2. "They go through a soft pull to verify their business and funding need"
       3. "Unqualified or fake requests get filtered out — you never see them"
       4. "Qualified referrals get sent to ONE broker — you"
       5. "You get their name, phone, email, funding amount, what it's for, and any notes from the qualification process"
     - Timeline visual: Use a vertical line connecting the numbered items (left border or a CSS pseudo-element), or cards stacked vertically with numbered circles on the left
     - Callout box (visually distinct — lighter background card or bordered card with accent):
       "Every referral is someone who actively requested funding. Your job is to reach them, build the relationship, and close the deal."
     - Navigation: "Back" (secondary) + "Next" (primary red)
     - Min-h-[44px] on buttons

2. Create `components/onboarding/steps/Step5BestPractices.tsx` (ONBD-07):
   - "use client"
   - Import shadcn: Button, Card, CardContent
   - Props: `{ onNext: () => void; onBack: () => void }`
   - No form state — pure content
   - Layout — ALL COPY MUST BE EXACTLY AS SPECIFIED:
     - Headline: "Brokers Who Close Follow These Rules"
     - 5 numbered cards, each with a bold title and body text:
       1. Title: "Speed matters."
          Body: "When a referral hits your phone, call within minutes — not hours. The faster you reach out, the more likely they remember submitting the request and the warmer the conversation starts."
       2. Title: "Lead with what you know."
          Body: "Say something like: 'Hey [Name], I'm reaching out about your funding request — I see you're looking for [amount] for [purpose]. Let's talk about getting that done.' You already have the details. Use them."
       3. Title: "Follow up at least 5 times."
          Body: "If they don't pick up, text immediately. Call again in a few hours. Then next morning. Then next afternoon. Then day 3. Most funded deals come from follow-up 2-4, not the first attempt."
       4. Title: "Don't pitch — match and move."
          Body: "They already have a funding need. Your job is to confirm the details, match them to the right product, and get docs moving. Keep it tight and efficient."
       5. Title: "Track your results."
          Body: "Let us know which referrals funded so we can optimize your lead quality over time. The more feedback we get, the better your referrals become."
     - Cards should have a large number on the left (styled, red accent) with title + body on the right
     - Navigation: "Back" (secondary) + "Next" (primary red)
     - Min-h-[44px] on buttons
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-onboarding && npx tsc --noEmit 2>&1 | head -20 && echo "---" && ls components/onboarding/steps/Step3HowItWorks.tsx components/onboarding/steps/Step5BestPractices.tsx</automated>
  </verify>
  <done>
    - Step 3 displays 5-step referral process timeline with callout box
    - Step 5 displays 5 numbered best practices cards
    - All copy matches the original spec verbatim
    - Both steps have Back/Next navigation with 44px+ touch targets
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Step 4 (ROI Calculator) and wire Steps 3-5 into OnboardingStepper</name>
  <files>
    components/onboarding/steps/Step4ROI.tsx
    components/onboarding/OnboardingStepper.tsx
  </files>
  <action>
1. Create `components/onboarding/steps/Step4ROI.tsx` (ONBD-06, DSGN-06):
   - "use client"
   - Import `Broker` from `@/lib/types`
   - Import shadcn: Button, Card, CardContent, Separator
   - Props: `{ broker: Broker; onNext: () => void; onBack: () => void }`
   - ROI calculation function (inside the component or as a helper):
     ```
     function calculateROI(dealAmount: number, batchSize: number) {
       if (!dealAmount || !batchSize || batchSize === 0) return null
       const pricePerReferral = dealAmount / batchSize
       const closedAt5Pct = Math.floor(batchSize * 0.05)
       const closedAt10Pct = Math.floor(batchSize * 0.10)
       const closedAt15Pct = Math.floor(batchSize * 0.15)
       return { pricePerReferral, closedAt5Pct, closedAt10Pct, closedAt15Pct }
     }
     ```
     IMPORTANT: Use `deal_amount / batch_size` for price per referral — do NOT hardcode $65. The $65 figure appears in the copy text only, not in the calculation.
   - Guard: If `calculateROI` returns null, show fallback text: "Contact Daniel for your personalized ROI breakdown." with Daniel's phone number.
   - Layout — ALL COPY FROM THE SPEC:
     - Headline: "Let's Set You Up for Success"
     - Section 1 — "How Referrals Work":
       "Every referral is a real business owner who submitted a funding request and went through a soft pull. They've been verified as someone with a real business and a real funding need. Some will have spoken with our team, some won't have — but all of them requested capital."
     - Section 2 — "Your Close Rate":
       "Brokers on our network close between 5% and 15% depending on skill, speed, and follow-up. That means if you buy 20 referrals, you should expect to fund 1-3 deals. At $65 per referral, that's a cost of $650-$1,300 per funded deal — and if your average commission is $3,000-$5,000+, the math works fast."
     - ROI VISUAL (DSGN-06 — make this visually impactful):
       - Large, bold, red-accented numbers that POP
       - Layout:
         - "{batch_size} referrals x ${pricePerReferral} = ${deal_amount}" (formatted with commas)
         - "At 5% close rate = {closedAt5Pct} funded deal(s)"
         - "Average commission = $3,000-$5,000"
         - "ROI: {roiLow}x - {roiHigh}x on your first batch"
         - Where roiLow = (closedAt5Pct * 3000) / dealAmount, roiHigh = (closedAt15Pct * 5000) / dealAmount
       - Use large text (text-3xl or text-4xl) for key numbers
       - Red accent color (#EF4444) for dollar amounts and ROI multiplier
       - Dark card background to make numbers stand out
       - If closedAt5Pct is 0 (very small batch), show at least "1" as minimum
     - Section 3 — "Not Every Referral Will Pick Up":
       "These are business owners, not people sitting by the phone waiting for your call. Some will answer on the first dial. Some will take 3-5 follow-ups. Some won't convert at all. That's the game. The brokers who win on this network are the ones who work every single referral with a real follow-up sequence — not the ones who call once and move on."
     - Section 4 — "This Is a Numbers Game":
       "Don't judge the network off 2 or 3 referrals. Give it a real sample — work all {batch_size} properly, and then evaluate. Every experienced broker knows that closing is about volume and consistency."
     - Navigation: "Back" (secondary) + "Next" (primary red)

2. Update `components/onboarding/OnboardingStepper.tsx` to wire in Steps 3-5:
   - Add imports for Step3HowItWorks, Step4ROI, Step5BestPractices
   - Replace the placeholder block for `currentStep >= 3 && currentStep <= 7` with:
     ```
     {currentStep === 3 && <Step3HowItWorks onNext={() => handleNext()} onBack={handleBack} />}
     {currentStep === 4 && <Step4ROI broker={broker} onNext={() => handleNext()} onBack={handleBack} />}
     {currentStep === 5 && <Step5BestPractices onNext={() => handleNext()} onBack={handleBack} />}
     ```
   - Keep placeholders for steps 6-7 only (Plan 04 will add those)
   - The placeholder for steps 6-7 should be:
     ```
     {currentStep >= 6 && currentStep <= 7 && (
       <div className="py-12 text-center text-muted-foreground">
         Step {currentStep} — coming soon
         <div className="mt-4 flex gap-2 justify-center">
           <Button variant="secondary" onClick={handleBack}>Back</Button>
           {currentStep < 7 ? (
             <Button onClick={() => handleNext()}>Next</Button>
           ) : (
             <Button onClick={() => handleComplete()}>Complete</Button>
           )}
         </div>
       </div>
     )}
     ```
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-onboarding && npx tsc --noEmit 2>&1 | head -20 && echo "---" && ls components/onboarding/steps/Step4ROI.tsx && grep -c "deal_amount" components/onboarding/steps/Step4ROI.tsx</automated>
    <manual>Step 4 shows personalized ROI numbers using broker's actual deal_amount and batch_size</manual>
  </verify>
  <done>
    - Step 4 ROI calculator uses broker.deal_amount / broker.batch_size (dynamic, not hardcoded $65)
    - ROI visual has large red-accented numbers that are visually impactful (DSGN-06)
    - Null/zero guards prevent NaN display
    - OnboardingStepper now renders Steps 1-5 with navigation
    - Steps 6-7 still have placeholders (Plan 04)
    - All copy matches the original spec exactly
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Steps 3, 4, 5 all render with correct copy from the spec
3. Step 4 ROI calculator uses actual broker deal_amount and batch_size
4. Step 4 handles edge case where batch_size or deal_amount is 0/null
5. OnboardingStepper renders steps 1-5 with proper navigation
6. All copy tone is direct, confident, money-motivated (DSGN-05)
</verification>

<success_criteria>
- Steps 3-5 display all spec copy verbatim
- ROI calculator personalizes numbers to broker's actual purchase
- ROI visual is large, bold, red-accented (DSGN-06)
- Navigation (Back/Next) works through steps 1-5
- No TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-flow/02-03-SUMMARY.md`
</output>
