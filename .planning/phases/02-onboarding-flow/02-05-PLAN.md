---
phase: 02-onboarding-flow
plan: 05
type: execute
wave: 4
depends_on: ["02-03", "02-04"]
files_modified: []
autonomous: false
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-05
  - ONBD-06
  - ONBD-07
  - ONBD-08
  - ONBD-09
  - ONBD-10
  - ONBD-11
  - ONBD-12
  - ONBD-13
  - GARD-01
  - GARD-02
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-05
  - DSGN-06

must_haves:
  truths:
    - "Full 7-step flow is visually correct on mobile viewport"
    - "Guard pages display correctly for invalid/completed tokens"
    - "Dark theme with red accent renders consistently across all steps"
    - "Build succeeds with zero errors"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 2 implementation by building the project and performing visual/functional verification of the full onboarding flow.

Purpose: Ensure all 7 steps, guard pages, dark theme, and completion flow work correctly end-to-end before marking Phase 2 complete.
Output: Verified working onboarding flow, build confirmation.
</objective>

<execution_context>
@/Users/haseeb/.claude/get-shit-done/workflows/execute-plan.md
@/Users/haseeb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-onboarding-flow/02-03-SUMMARY.md
@.planning/phases/02-onboarding-flow/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run production build and fix any errors</name>
  <files></files>
  <action>
1. Run `npm run build` in the project root
2. If there are TypeScript or build errors:
   - Fix them in the relevant source files
   - Re-run `npm run build` until it passes
3. Common issues to watch for:
   - Missing imports (shadcn components may need specific import paths)
   - Motion components in Server Components (all motion usage must be in "use client" files)
   - Zod v4 import differences
   - Type mismatches between Broker interface and form data
4. Verify build output shows all pages compiled successfully
5. Commit any build fixes with a descriptive message
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-onboarding && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `npm run build` completes with zero errors
    - All routes compile: /onboard/[token], /onboard/[token]/complete, /error
    - All API routes compile: /api/brokers/[token]/start, /api/brokers/[token]/step, /api/brokers/[token]/complete
  </done>
</task>

<task type="auto">
  <name>Task 2: Visual and functional verification of complete onboarding flow</name>
  <files></files>
  <action>
1. Create a test broker record by calling the webhook endpoint:
   ```
   curl -X POST http://localhost:3000/api/webhooks/ghl \
     -H "Content-Type: application/json" \
     -d '{"ghl_contact_id":"test-verify-001","first_name":"Test","last_name":"Broker","email":"test@example.com","phone":"7025551234","company_name":"Test Funding LLC","state":"NV","primary_vertical":"MCA","secondary_vertical":"SBA","batch_size":20,"deal_amount":1300}'
   ```
2. Extract the onboarding_url from the response
3. Start the dev server if not running: `npm run dev`
4. Use the dev server URL to verify the flow loads correctly
5. Report the verification results to the user for their manual check

Human verification is needed to confirm:
- Visual appearance matches the dark theme with red accent
- Copy text matches the original spec
- Mobile viewport layout is correct
- Step navigation animation feels smooth
- ROI calculator displays personalized numbers correctly
  </action>
  <verify>
    <automated>cd /Users/haseeb/ppl-onboarding && curl -s -X POST http://localhost:3000/api/webhooks/ghl -H "Content-Type: application/json" -d '{"ghl_contact_id":"test-verify-phase2","first_name":"Test","last_name":"Broker","email":"test@example.com","phone":"7025551234","company_name":"Test Funding LLC","state":"NV","primary_vertical":"MCA","batch_size":20,"deal_amount":1300}' 2>/dev/null || echo "Dev server not running — run npm run dev first"</automated>
  </verify>
  <done>
    - Test broker created via webhook endpoint
    - User visually verified the complete 7-step onboarding flow
    - Guard pages confirmed working for invalid and completed tokens
    - Dark theme with red accent renders consistently
    - Mobile viewport verified
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. All 7 steps render correctly with spec copy
3. Guard pages work for invalid and completed tokens
4. Dark theme with red accent is consistent
5. Mobile viewport renders correctly
6. Completion flow triggers database update and GHL webhook
</verification>

<success_criteria>
- Production build succeeds
- Full 7-step flow verified visually on mobile viewport
- Guard pages confirmed working
- Completion API confirmed firing
- User approves the visual/functional verification
</success_criteria>

<output>
After completion, create `.planning/phases/02-onboarding-flow/02-05-SUMMARY.md`
</output>
